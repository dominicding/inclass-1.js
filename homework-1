function setup() {
  createCanvas(200,200);
  background(0);
}

function draw() {
  for(var i=0; i<20; i=i+1){
    ellipseMode(CENTER);
    ellipse(i*10, i*10, 10,10);
    fill(255);
    stroke(255);
  }
}
