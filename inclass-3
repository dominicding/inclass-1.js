function setup() {
  createCanvas(500,500);
  background(255);
}

function draw() {
    for(var i=0; i<500;i=i+10){
  line(i+10,10,250,250);
  }
  for(var i=0; i<500;i=i+10){
  line(i+10,500,250,250);
  }
}
